server:
  port: 1234

spring:
  profiles:
    # 要和pom.xml配合使用
    active: @profileActive@
  #spring:
  #  profiles:
  #    active: dev,common
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
